[project]
name = "shopping-cart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Guffran2004-ops", email = "guffranjg.shaik@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
    "pymongo>=4.18.1",
]

[project.scripts]
shopping-cart = "shopping_cart:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
